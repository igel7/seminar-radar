# 多言語化計画 — 英語版・ドイツ語版の追加と切替UI

## 結論(推奨方針)

**HTMLは1枚のまま、ページ内のJS辞書で日本語/英語/ドイツ語を切り替える方式**にする。
ヘッダーに `JA | EN | DE` の切替ボタンを置き、選択は localStorage に記憶、
`?lang=en` のようなURLパラメータでも指定できるようにする。

3枚のページ(`index.html` / `en/` / `de/`)を生成する案は採らない。理由:

- このサイトは毎日自動再生成されるため、テンプレートが3枚になると保守コストと
  生成ロジックの複雑さが3倍になる
- イベントデータ・フィルタ機能は全言語で同一。違うのはUI文字列だけ
- 画面は既に全てJSで描画している(カレンダー・一覧・プルダウン)ので、
  言語切替は「辞書を差し替えて `redraw()`」するだけで済み、既存設計と相性がよい

## 現状整理

| 項目 | 現状 |
|---|---|
| ページ生成 | `scripts/template.html` を `scripts/radar_lib.py` がプレースホルダ置換して `docs/index.html` を出力 |
| UI文字列 | template.html 内のHTMLとJSに日本語がハードコード(フィルタ、凡例、バッジ、footer、調査対象パネル等) |
| イベントデータ | `title`(原文=英語/ドイツ語)は言語非依存。`title_ja`・`summary_ja`・`registration_note` は日本語専用 |
| 国名・曜日・月 | `COUNTRY_JA` / `COUNTRY_KANA` 辞書、`WD` / `WD_WEEK` 配列、「2026年7月」形式の見出し |

## 実装ステップ

### 1. UI文字列の辞書化(template.html)

- ハードコードされた日本語を `I18N = {ja:{…}, en:{…}, de:{…}}` に集約し、
  参照関数 `t(key)` に置き換える。対象:
  - フィルタのラベルと選択肢(「すべて」「ドイツ以外」「①中銀」…「★1以上 — 標準」等)
  - チェックボックス(「オンライン開催を含む」「Frankfurt日帰り圏のみ」)
  - カレンダー凡例・説明文、フィルタ適用中バー、「今日」ボタン
  - 一覧のバッジ(「登録参加可」「無料」「有料」「招待制」「オンライン」等)と
    「申込:」「申込ページ →」「情報源:」「初検出:」
  - セクション見出し(「今後のイベント一覧」「巡回ステータス」)、空表示、footer、
    タグライン、調査対象パネル(対象地域・広域検索の説明文)
- HTML静的部分(タグライン・フィルタラベル等)は初期化時と切替時に書き換える
  `applyStaticText()` を追加。動的部分は既存の `redraw()` が辞書参照するだけ
- 曜日・月表記: `WD` / `WD_WEEK` を言語別配列に。月見出し「2026年7月」→
  "July 2026" / "Juli 2026"
- 国名: EN/DE では `Intl.DisplayNames(['en'|'de'], {type:'region'})` を使い
  ISOコードから自動生成(辞書追加不要・全対応)。日本語は既存 `COUNTRY_JA` を継続。
  Organizer括弧内の `COUNTRY_KANA` も同様に Intl へ寄せる
- 言語バッジ: 「英/独」→ "EN/DE"、`evLabel()` 内の「英・」プレフィックスも言語別に
- Organizerグループ見出し(「中銀・規制当局」等)も辞書化

### 2. 切替UI

- ヘッダー右側(検索欄の隣)に `JA | EN | DE` トグルを追加(既存 `.srcbtn` と同系スタイル)
- 選択を localStorage(既存フィルタ保存とは別キー)に保存。次回訪問時も維持
- `?lang=en` / `?lang=de` で起動指定可(localStorage より優先し、その値を保存)
- 切替時に `<html lang>` と `<title>` も更新

### 3. 日本語専用データフィールドの扱い(要決定 → 下記「決めてほしい点」)

- `title`(原文)は全言語共通で表示。EN/DE 時は `title_ja` 行を非表示にするだけ
- `summary_ja`(日本語要約)と `registration_note`(日本語メモ)は選択肢が2つ:
  - **案a(最小)**: EN/DE 時は非表示。イベント名・主催者・日付・バッジ・リンクは
    全て言語非依存なので、実用上はこれだけでも十分機能する。即日リリース可
  - **案b(推奨)**: `AGENTS.md` のイベントスキーマに `summary_en`(英語要約1〜2文)を
    追加し、毎日の自動更新で新規イベントに付与。既存イベントは一度だけ
    バックフィル(既存 `summary_ja` と原文タイトルから英訳を追記)。
    EN・DE 両モードで `summary_en` を表示する(対象セミナー自体が英語/ドイツ語
    開催なので、閲覧者にとって英語要約で実用十分。ドイツ語専用要約まで作るのは
    コスト対効果が低く見送りを推奨)
  - `registration_note` も同様に `registration_note_en` を追加するか、
    EN/DE 時は非表示(登録URLバッジ・「登録参加可」バッジは言語非依存で残る)
- `organizer_short` に日本語が混じるケース(例「Bundesbank/ECB他」)は、
  表示時に「他」→ "+" などの軽い置換で対応し、あわせて `AGENTS.md` の
  抽出ルールに「organizer_short はASCIIで」と明記して今後の混入を防ぐ

### 4. Python側(radar_lib.py / ingest.py)

- テンプレート置換の仕組みは変更不要
- 案bの場合のみ: 新フィールドをマージ対象に追加(既存の「Claude抽出は空欄を
  埋める」ロジックに `summary_en` を乗せる)
- `docs/calendar.ics` の説明文(現在 `summary_ja` を使用)は当面日本語のまま。
  希望があれば `summary_en` 優先に変更可能

### 5. 検証

- ローカルで `ingest.py` を実行して `docs/index.html` を再生成し、3言語それぞれで
  カレンダー・週ビュー(モバイル)・一覧・フィルタ連動・調査対象パネル・
  巡回ステータスの表示を確認
- 既存ユーザーの localStorage(フィルタ保存値 `sr_filters_v5`)を壊さないことを確認
- 切替が1クリックで全画面に反映されること、リロード後も言語が維持されることを確認

## 規模感

- **案a**: template.html の改修が中心。1回の作業セッションで完了する規模
- **案b**: 上記+ `AGENTS.md` 数行の追記+既存イベントの一括英訳(1回だけ)。
  翌日以降は毎日の自動更新が新規イベントに英語要約を付ける

## 決めてほしい点

1. **要約の扱い**: 案a(EN/DE では要約非表示・最小コスト)か、
   案b(英語要約 `summary_en` を追加・推奨)か
2. **デフォルト言語**: 初回訪問は従来どおり日本語でよいか
   (ブラウザ言語からの自動判定にすることも可能)
3. **ドイツ語要約**: DE モードでも英語要約の共用でよいか(推奨)、
   それとも `summary_de` まで作るか
