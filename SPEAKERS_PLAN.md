# 登壇者(speakers)フィールド追加計画

> 状態: **未実装(計画のみ)**。実装後はこの見出しを「実装済み」に書き換える。

## 目的と割り切り

- 巡回時に**登壇者名を構造化して記録する**。現状は要約文やタイトルに偶発的に混じるだけで、
  フィールドとしては存在しない(`AGENTS.md` のスキーマ参照)。
- 将来「ECB要人」「ブンデスバンク要人」のような**所属・地位によるフィルタ**を作る際の
  素材にする。ただし**「要人か否か」の判定は今回やらない**。判定基準が曖昧で、日次の
  LLM抽出に委ねると日によってブレるため。後日、名簿ファイル(ユーザー管理)との照合を
  `ingest.py` 側で行う方式で載せる想定(LLM不使用・判定が安定する)。
- トークン消費への影響は小さい見込み(後述)。増える可能性があるのは「登壇者だけが
  変わったイベントの再出力」なので、そこだけ仕組みで抑える。

## スキーマ設計

`speakers`: 配列。各要素は次のオブジェクト。ページに登壇者の記載がなければ **`[]`**。

| キー | 内容 |
|---|---|
| `name` | 氏名(原文表記。肩書き・敬称(Dr., Prof.)は付けない)。**必須** |
| `org` | 所属機関の略称。**`organizer_short` と同じ表記規則**(ラテン文字のみ・定着した略称。例 `"ECB"`, `"Bundesbank"`, `"BIS"`, `"IfW Kiel"`)。ページに所属がなければ `null` |
| `role` | 役職の短い原文(例 `"President"`, `"Executive Board Member"`, `"Vizepräsidentin"`)。なければ `null` |

設計上の判断:

- `org` に `organizer_short` の規則を流用するのは、将来のフィルタが主催フィルタと同じ
  「機関トークンの完全一致」で動けるようにするため(表記が割れると機関単位で拾えない)。
- 名前だけの文字列配列にしない。所属を後から名前で復元するのは名簿がないと不可能で、
  名簿方式にしても同姓同名や表記揺れで崩れる。抽出時点で所属が書いてあるなら取っておく。
- **上限 8 名**。それ以上並ぶ大規模会議(数十名の登壇者一覧)は、基調講演者・パネルの
  中銀/政府/国際機関の高官・主催機関の首脳を優先して 8 名まで。数を埋めるために
  取るのではなく「誰が出るかで価値が決まる」人を取る。研究セミナーの報告者は 1 名で完結。
- `speakers` はイベントの**収載可否・重要度の判定には使わない**(現行ルールのまま)。

## 変更箇所

### 1. `AGENTS.md`(スキーマ・抽出ルール・照合ルール)

- スキーマ表に `speakers` の行を追加(上の表の内容)。
- 抽出ルールに追加:
  - ページに書かれている登壇者のみ。**推測・補完しない**(「毎年総裁が出るから」で
    名前を入れない)。TBA/TBC は `[]`。
  - モデレーター・司会は原則含めない(枠が余っていて明示されていれば可)。
  - `org` の表記は `organizer_short` と同じ規則。主催機関の人物なら `organizer_short`
    と同じ文字列を使う。
  - 上限 8 名と優先順位(上記)。
- 手順4(既知イベントとの照合)に追加: **登壇者だけが新しく判明/変化したイベントは
  「薄い再出力」でよい**(下記「差分検知」参照)。
- A-3 のメインモデル検証項目に「`speakers` の捏造(ページにない人名)がないか」を追加。

### 2. `scripts/radar_lib.py`

- `sanitize()`: `speakers` を正規化する。
  - 配列でなければ `[]`。要素が文字列なら `{"name": s, "org": null, "role": null}` に変換
    (サブエージェントが手を抜いた場合の受け皿)。
  - `name` が空の要素は捨てる。前後空白を除去。`org`/`role` は非空文字列以外 `null`。
  - `name` の大文字小文字を無視した重複を除去。**先頭 8 件で打ち切り**。
- `merge()`: 変更不要。既存の「非空なら新値で上書き」に乗る(`[]` は `not in (None, "", "unknown", [])`
  で弾かれるので、空リストで既存の登壇者を消すことはない。最新の登壇者リストで
  上書きされる)。
- `CHANGELOG_WATCHED_FIELDS` には**入れない**。更新履歴(News)に「登壇者更新」が
  大量に載るのを避ける。後で載せたくなったら追加するだけでよい。
- `_changelog_snap()`: 変更なし。
- `render_ics()`: `DESCRIPTION` の末尾に `/ 登壇: A, B` を付ける(任意。ICS は importance 0 を
  含まないので肥大化しない)。

### 3. `scripts/ingest.py` — `write_known_digest()`

- 台帳のキーに `speakers` を追加する。ただし**名前だけの配列**に潰して載せる
  (`["Joachim Nagel", "Isabel Schnabel"]`)。目的は「前回すでに取れているか」「顔ぶれが
  変わったか」を巡回エージェントが `grep` で判定できるようにすること。
  `org`/`role` まで載せると台帳が太り、毎セッション・全サブエージェントが読む
  入力トークンが増える。

### 4. 差分検知と「薄い再出力」(トークン増加を抑える要)

登壇者リストは開催が近づくにつれ更新されるため、「観察可能な変化」として扱うと既知
イベントの再出力が増える。再出力のたびに 3 言語要約を再生成するのが最大の無駄なので、
次のルールを `AGENTS.md` 手順4に置く:

- 既知イベントで**登壇者以外に変化がない**場合は、次のキーだけの「薄いレコード」を
  出力してよい: `title`(台帳と**完全に同じ表記**)、`date_start`(台帳と同じ)、
  `themes`(既存と同じでよい)、`source`、`url`、`speakers`。
  `summary_*` / `registration_note_*` / `title_ja` 等は**省略する**(`null` でもよい)。
- これで `merge()` は `event_key`(タイトル+開始日のハッシュ)で既存レコードに当たり、
  `speakers` だけ上書きし、既存の要約は `None` スキップで保持される。
- リスク: タイトルや日付を台帳と違う表記で書くと、要約のない新規イベントとして
  登録されてしまう。緩和策として (a) 台帳の表記をコピーするよう明記、(b) `sanitize()` で
  「`summary_ja` も `summary_en` も無い新規レコード」は**新規追加を拒否する**
  (既存マッチしなかった薄いレコードは捨てて標準エラーに警告を出す)。(b) は
  `merge()` の新規追加分岐に条件を 1 つ足すだけで実装できる。

### 5. `scripts/template.html`(表示・検索)

最小限だけ入れる。フィルタUIは作らない(要人判定が決まってから)。

- 一覧の行に `speakers` があれば 1 行追加: `登壇: Joachim Nagel (Bundesbank), …`。
  `org` があれば括弧で添える。I18N ラベル `speakersLbl` を ja/en/de に追加
  (`"登壇:"` / `"Speakers:"` / `"Referenten:"`)。
- `passQ()` の検索対象(haystack)に `speakers` の `name` と `org` を加える。
  これだけで「Nagel」「ECB」とフリーテキスト検索すれば登壇イベントが引ける。
  要人フィルタが無くても当面の実用にはこれで足りる。

### 6. その他

- `scripts/update_api.py` の `EVENT_SCHEMA_NOTE`(API従量課金版の予備手段)にも
  `speakers` の 1 行を足して同期しておく(使っていないが齟齬を残さない)。
- `README.md` はスキーマを列挙していないので変更不要。
- `data/overrides.json` は既存機構で `speakers` も上書きできる(キーを問わない実装なら)。
  実装時に `apply_overrides()` を確認する。

## 既存データ・後方互換

- 既存の約 560 件には `speakers` が無い(キー欠落)。テンプレートは `ev.speakers || []`
  で読み、無ければ何も表示しない。遡及抽出(バックフィル)は**行わない**。
  重要度の高い直近イベントで欲しければ、URL を `INBOX.md` に貼れば手順2.5で薄い
  再出力として取り込める。
- `data/archive.json` / `changelog.json` は無変更。

## トークン影響(推測。根拠は `data/token_usage.json` の直近実績)

- 直近の日次巡回は合計 15〜45M トークン、出力 10〜90 万トークン。
- 入力: `AGENTS.md` の追記 +300 トークン程度 × (メイン + サブエージェント数)、
  台帳の名前配列で +数千トークン。日次総量に対して 1% 未満。
- 出力: 新規イベント 1 件あたり +30〜120 トークン(8 名上限のオブジェクト配列)。
  1 件の現行出力が数百トークンなので新規分で +10〜20%。日次総量では数%。
- 再出力: 「薄い再出力」により登壇者だけの更新は 1 件 100 トークン前後で済む。
  導入後 1〜2 週間、`python3 scripts/log_tokens.py --summary` で前後比較する。

## 検証手順(実装時)

1. `radar_lib.sanitize()` に対して、文字列配列・オブジェクト配列・重複・9 名以上・
   空要素の入力を与えて正規化結果を確認する(標準ライブラリだけの一時スクリプトで可)。
2. `data/events.json` のコピーに対して、(a) `speakers` 付き新規イベント、(b) 既知イベントの
   薄い再出力、(c) タイトルを崩した薄いレコード、を `merge()` に通し、(a) 追加、
   (b) 既存レコードの `speakers` だけ更新・要約保持、(c) 拒否+警告、になることを確認する。
   **`data/` 本体には書かない**(A-4)。
3. `python3 scripts/ingest.py --maintenance` で `docs/index.html` を再生成し、
   `speakers` 無しの既存データで表示崩れがないことを確認する(`docs/index.html` は
   読まない。ブラウザ確認は Playwright のスクリーンショットで行う)。
4. `AGENTS.md` の追記を通しで読み、手順2のサブエージェント指示文に登壇者ルールが
   含まれる構成になっているか確認する。

## 決めてほしい点(既定値のままなら返事不要)

1. **上限 8 名**でよいか(5 に絞るとより保守的。大規模会議の高官パネルは 5 を超えがち)。
2. **更新履歴(News)に登壇者更新を載せない**でよいか(既定: 載せない)。
3. **一覧表示は入れる**でよいか(既定: 入れる。検索対象への追加だけにもできる)。
